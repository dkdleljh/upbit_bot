# 업비트봇 사용설명서 (초보자용)

이 문서는 `upbit_bot`을 **처음 설치하고 안전하게 실행**하기 위한 설명서입니다.

> ⚠️ 면책
> - 투자 조언이 아닙니다.
> - 실거래 전에는 반드시 paper/backtest로 검증 후 소액으로 시작하세요.

---

## 0) 폴더 위치

- `~/Desktop/upbit_bot`

---

## 1) 처음 1회 설치

```bash
cd ~/Desktop/upbit_bot
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

---

## 2) API 키(.env) 설정 — 절대 GitHub에 올리면 안 됩니다

```bash
cd ~/Desktop/upbit_bot
cp .env.example .env
nano .env
```

필수:
```env
UPBIT_ACCESS_KEY=...
UPBIT_SECRET_KEY=...
```

(선택) 알림:
- Slack/Telegram/Webhook

---

## 3) 실행 모드(중요)

이 봇은 3가지 모드가 있습니다.

- `paper`: 모의매매(처음은 반드시 여기)
- `backtest`: 과거 데이터 테스트
- `live`: 실거래

### 3.1 paper 실행(권장)
```bash
python -m src.main --mode paper
```

### 3.2 backtest 실행
```bash
python -m src.main --mode backtest
```

### 3.3 live 실행(실거래)
```bash
python -m src.main --mode live
```

> 안전장치
> - 환경변수 `UPBIT_LIVE_CONFIRM=YES`가 없으면 live가 차단될 수 있습니다.
> - `UPBIT_KILL_SWITCH=1`이면 주문이 거부됩니다.

---

## 4) 상태 점검(Health)

```bash
python -m src.main --health
```

---

## 5) 로그/데이터 위치

- 로그: `logs/`
- DB: `data/trading_bot.db` (SQLite)
- 리포트: `reports/`

---

## 6) 실전 운영 체크리스트

- [ ] `.env`가 커밋되지 않았는지 확인 (`git status`)
- [ ] 처음은 paper로 1~2일 이상 검증
- [ ] live는 소액으로 시작
- [ ] 이상하면 `UPBIT_KILL_SWITCH=1`로 즉시 차단

---

## 7) 트러블슈팅

### Q1. 실행이 바로 종료돼요
- `.env` 키 누락 여부 확인
- `--health`로 의존성/설정 확인

### Q2. 주문이 안 나가요
- `UPBIT_LIVE_CONFIRM` / `UPBIT_KILL_SWITCH` 확인
